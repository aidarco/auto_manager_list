import 'package:flutter/material.dart';


class ClassisPage extends StatelessWidget {
  const ClassisPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("Шасси"),);
  }
}
