import 'package:flutter/material.dart';

class EnginePage extends StatelessWidget {
  const EnginePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Двигатель"),
    );
  }
}
